{
  "name": "ascii2mathml",
  "version": "0.2.6",
  "description": "The second AsciiMath – MathML converter",
  "repository": {
    "type": "git",
    "url": "https://github.com/runarberg/ascii2mathml"
  },
  "main": "index.js",
  "bin": {
    "ascii2mathml": "bin/index.js"
  },
  "scripts": {
    "prebuild": "npm run lint; rm -fr lib; mkdir lib",
    "build": "babel src/ -d lib/; babel < index.es6.js > index.js",
    "predist": "npm run lint; rm -fr dist; mkdir dist",
    "dist": "browserify ./index.es6.js -s ascii2mathml > dist/ascii2mathml.js",
    "lint": "eslint --reset index.es6.js src/",
    "prepublish": "npm run build",
    "postpublish": "npm run dist",
    "pretest": "npm run build",
    "test": "mocha"
  },
  "keywords": [
    "mathml",
    "asciimath",
    "asciimathml"
  ],
  "author": "Rúnar Berg Baugsson Sigríðarson",
  "license": "MIT",
  "browserify": {
    "transform": [
      "babelify"
    ]
  },
  "devDependencies": {
    "babelify": "^5.0.4",
    "browserify": "^9.0.3",
    "eslint": "^0.24.0",
    "eslint-plugin-nodeca": "^1.0.3",
    "expect.js": "^0.3.1",
    "mocha": "^2.1.0"
  },
  "dependencies": {
    "babel": "^5.0.6",
    "minimist": "^1.1.2"
  }
}
